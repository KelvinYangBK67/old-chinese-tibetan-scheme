/* 只收錄有文獻依據的逐項例外；鍵可用漢字、詞條 ID 或原始 B–S。 */
globalThis.BS_TIBETAN_DATA = Object.freeze({
  voicelessSonorantRewrite: Object.freeze({
    /* 例如："某字或 row ID": "k-ma"。不可只按 m̥ 等字符全局替換。 */
  }),
  specialNormalization: Object.freeze({})
});

